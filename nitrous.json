{
  "template": "jekyll",
  "ports": [3000],
  "name": "Pixyll",
  "description": "",
  "scripts": {
    "post-create": "bundle install",
    "Start Pixyll": "cd ~/code/pixyll && jekyll serve --host 0.0.0.0 --port 3000"
  }
}
